This document defines a standardized method for logging decisions to allow or deny API requests to enable [=reconstruction=], analysis and [=replay=] of historical decisions.
The standard builds on the information models defined by [[AuthZen]] to log decisions.
