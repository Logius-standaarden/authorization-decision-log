This document defines a standardized method for logging decisions to allow or deny API requests, to provide accountability both within and across organisations.
The standard builds on the information models defined by [[AuthZEN]] to enable [=reconstruction=], analysis and [=replay=] of historical decisions.
